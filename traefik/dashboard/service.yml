apiVersion: v1
kind: Service
metadata:
  name: traefik-dashboard
  annotations:
    metallb.io/loadBalancerIPs: 10.1.1.5
spec:
  selector:
    app.kubernetes.io/name: traefik
    app.kubernetes.io/component: proxy
  type: LoadBalancer
  ports:
    - name: internal
      port: 8080
      targetPort: 8080